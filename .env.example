# Database
DATABASE_URL="mysql://user:password@localhost:8889/pdf_editor"

# API Security
# This is the master API key used for:
# 1. Registering new domains via /api/domains/register
# 2. Managing domain registrations (update, delete, regenerate keys)
# 3. Backward compatibility for internal API calls
API_KEY="your-secure-master-api-key-here"

# File Upload
MAX_FILE_SIZE=10485760
UPLOAD_DIR="./public/uploads"

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NODE_ENV="development"
